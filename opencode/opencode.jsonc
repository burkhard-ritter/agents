{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "amazon-bedrock": {
      "options": {
        "region": "eu-central-1",
      },
    },
  },
  // Disable built-in bash tool — use run_code instead
  "permission": {
    "bash": "deny",
  },
}
